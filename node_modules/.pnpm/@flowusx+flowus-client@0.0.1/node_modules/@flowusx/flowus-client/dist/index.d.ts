import { PageBlocks } from '@flowusx/flowus-types';

interface FlowUsConfig {
    baseUrl?: string;
}

/**
 * FlowUs客户端API
 */
declare class FlowUsClient {
    private readonly _baseUrl;
    constructor(config?: FlowUsConfig);
    /**
     * 获取页面所有Block
     * @param pageId
     */
    getPageBlocks(pageId: string): Promise<PageBlocks>;
    /**
     * 获取数据表格文档列表
     * @param pageId
     */
    getDataTableData(pageId: string): Promise<PageBlocks>;
    private _fetch;
    private getOssUrl;
}

export { FlowUsClient };
