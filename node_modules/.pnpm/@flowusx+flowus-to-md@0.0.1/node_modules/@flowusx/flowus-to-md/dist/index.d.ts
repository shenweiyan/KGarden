import { PageBlocks } from '@flowusx/flowus-types';
import { FlowUsClient } from '@flowusx/flowus-client';

interface ConfigOptions {
    saveChildPage?: boolean;
}
interface FlowUsToMarkdownOptions {
    client?: FlowUsClient;
    config?: ConfigOptions;
}

/**
 * FlowUs文档转Md
 */
declare class FlowUsToMarkdown {
    private readonly flowusClient?;
    private readonly config?;
    constructor(options?: FlowUsToMarkdownOptions);
    pageToMarkdown(id: string): Promise<string>;
    toMarkdownString(pageBlocks: PageBlocks): string;
}

export { FlowUsToMarkdown };
